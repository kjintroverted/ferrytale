import styled from 'styled-components';

export const Spacer = styled.span`
    flex: 1;
`

export const Header = styled.h3`
  color: white;
  padding: 10px;
  background: linear-gradient(to right, teal, blue, purple);
`